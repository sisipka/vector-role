---
dependency:
  name: galaxy
  enabled: true
driver:
  name: podman
platforms:
  - name: instance
    image: docker.io/pycontribs/ubuntu:latest
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
